const player = 'player';
const select = 'select';
const message = 'message';
const messages = 'messages'
const namespace = 'namespace';
const session = 'session';
const ready = 'ready';
const resume = 'resume';
const SET = "SET"
const CLEAR = "CLEAR"
const UPDATE = "UPDATE";

export {player, select, message, messages, namespace, session, ready, resume, SET, CLEAR, UPDATE}