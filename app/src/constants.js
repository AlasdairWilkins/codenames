const player = 'player';
const select = 'select';
const message = 'message';
const namespace = 'namespace';
const session = 'session';
const ready = 'ready';
const resume = 'resume';
const SET = "SET"
const CLEAR = "CLEAR"

export {player, select, message, namespace, session, ready, resume, SET, CLEAR}